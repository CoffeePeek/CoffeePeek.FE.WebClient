# CoffeePeek App Development

This is a code bundle for CoffeePeek App Development. The original project is available at https://www.figma.com/design/rkXySHKOyJIf6RPgMDyMOK/CoffeePeek-App-Development.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения (опционально)

Создайте файл `.env` в корне проекта (можно скопировать из `.env.example`):

```bash
# Для использования production API (по умолчанию)
VITE_API_BASE_URL=https://nginx-dev-c5be.up.railway.app
VITE_USE_LOCAL_API=false

# Или для использования локального API
# VITE_USE_LOCAL_API=true
```

**Примечание:** Если файл `.env` не создан, приложение будет использовать production API по умолчанию.

### 3. Запуск приложения

```bash
npm run dev
```

Приложение откроется автоматически в браузере на `http://localhost:3000`

### 4. Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `build/`

## 📋 Требования

- Node.js версии 18 или выше
- npm или yarn

## 🔧 Доступные команды

- `npm run dev` - запуск dev сервера
- `npm run build` - сборка для production

## 📡 API

Приложение интегрировано с CoffeePeek API:
- Production: `https://nginx-dev-c5be.up.railway.app`
- Local: `http://localhost:80`

**Важно:** В режиме разработки (`npm run dev`) все запросы к API автоматически проксируются через Vite dev сервер, что решает проблемы с CORS. В production сборке запросы идут напрямую к API.

### 🔧 Решение проблем с CORS

Если вы видите ошибки CORS при запуске приложения:

1. **Убедитесь, что используете `npm run dev`** - прокси работает только в режиме разработки
2. **Проверьте, что прокси настроен** в `vite.config.ts` (уже настроен)
3. **Перезапустите dev сервер** после изменений в конфигурации:
   ```bash
   # Остановите сервер (Ctrl+C) и запустите снова
   npm run dev
   ```

Прокси автоматически перенаправляет все запросы к `/api/*` и `/favorite*` на production сервер, избегая проблем с CORS.

Все эндпоинты API доступны через сервисы в `src/api/`:
- `authApi` - аутентификация
- `userApi` - управление пользователями
- `coffeeshopApi` - кофейни
- `reviewApi` - отзывы
- `moderationApi` - модерация
- `internalApi` - внутренние API

## 🐛 Решение проблем

Если возникают ошибки при запуске:

1. Убедитесь, что все зависимости установлены: `npm install`
2. Проверьте версию Node.js: `node --version` (должна быть 18+)
3. Очистите кэш и переустановите зависимости:
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```
4. Проверьте, что порт 3000 свободен
